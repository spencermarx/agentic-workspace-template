{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "plan.json",
  "description": "Reference, not enforcement: nothing loads this file, `load_plan` is a bare `json.loads`. It documents the desired folder structure as a flat, ordered node list. Flat because a diff must be readable and because `add` is a single append: that is what makes bootstrap and new-area the same code.",
  "type": "object",
  "properties": {
    "$schema": { "type": "string" },
    "nodes": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "path": {
            "type": "string",
            "description": "Slash-separated, relative to the repo root. The folder is created at this path."
          },
          "role": {
            "type": "string",
            "description": "router: has children with their own CLAUDE.md, points down only. leaf: fat context, full section skeleton. plain: ambient folder, no CLAUDE.md."
          },
          "title": {
            "type": "string",
            "description": "Display name. Fills {{NODE_TITLE}} and the folder map row."
          },
          "holds": {
            "type": "string",
            "description": "One phrase for the folder map's 'What it holds' column. Without it the row repeats the title, which spends a column saying nothing."
          },
          "instanceScaffold": {
            "type": "array",
            "items": { "type": "string" },
            "description": "Router only. Sub-folders every instance added under this router gets. Defaults to Activities/Documents, which is a domain's shape and wrong for an operator."
          },
          "instanceChildren": {
            "type": "array",
            "items": { "type": "string" },
            "description": "Router only. Child folders every instance gets, such as decisions/ for a business domain."
          },
          "children": {
            "type": "array",
            "items": { "type": "string" },
            "description": "Sub-folder names scaffolded under this node, each with a .gitkeep."
          },
          "files": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "name": { "type": "string" },
                "template": {
                  "type": "string",
                  "description": "Basename of a file in .workspace/templates/, without the .md."
                }
              }
            },
            "description": "Files rendered into the node's folder at scaffold time."
          },
          "gitkeep": {
            "type": "boolean",
            "description": "Put a .gitkeep in the node's own folder, so an empty ambient folder survives a clone."
          },
          "instanceRole": {
            "type": "string",
            "description": "The role each child instance gets when `add` appends one under this node."
          },
          "instanceTemplate": {
            "type": "boolean",
            "description": "Marks this node as a parent instances are added under. Only its presence is read; `new-area` uses it to infer the parent from a name without asking."
          },
          "scaffold": {
            "type": "array",
            "items": { "type": "string" },
            "description": "The two-bucket sub-shape, conventionally [\"Activities\", \"Documents\"]."
          },
          "slots": {
            "type": "object",
            "properties": {
              "purpose": {
                "type": "string",
                "description": "One line saying what the node is for. Fills {{NODE_PURPOSE}} and the README of a plain folder."
              }
            }
          }
        }
      }
    }
  }
}
