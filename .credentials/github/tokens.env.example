# GitHub. Most operations use the `gh` CLI, which carries its own auth, so this
# file is only needed for raw API calls that bypass it.
#
# Obtain: https://github.com/settings/tokens  (fine-grained, repo scope)
GITHUB_TOKEN=__REPLACE_ME__
